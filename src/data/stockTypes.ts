export interface VolumeStock {
  rank: number;
  name: string;
  price: number;
  change: number;
  volume: number;
}

export interface VolumeCountStock {
  rank: number;
  name: string;
  price: number;
  change: number;
  volume_count: number;
}
